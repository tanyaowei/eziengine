{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debugUbuntuBuild",
            "type": "shell",
            "command": "clear; bash ${workspaceFolder}/buildubuntu.sh Debug",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "releaseUbuntuBuild",
            "type": "shell",
            "command": "clear; bash ${workspaceFolder}/buildubuntu.sh Release"
        }
    ]
}